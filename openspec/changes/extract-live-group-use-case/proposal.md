# Extract Live Group Loading Use Case

Move lazy group loading orchestration from the route handler into `application/live` without changing the catalog model, UI, Howen integration, or group-lazy behavior.
