# Tasks: Multi-provider canonical catalog

## Review Workload Forecast

Estimated lines: 6,500-9,000. Delivery: auto-chain.

Decision needed before apply: No
Chained PRs recommended: Yes
Chain strategy: feature-branch-chain
400-line budget risk: High

### Suggested Work Units

PR1 targets tracker; successors target predecessors; <=400 lines each.

17 PRs: core; Company binding; Fleet union; matching; precedence; Mongo hierarchy; Mongo identities; Cybermapa contract/import; Howen; sync orchestration; run/lease; cron; admin API/status; Spanish UI; live; docs/quality.

## Phase 1: Core

- [x] 1.1 **RED:** Test isolation, provider/native Vehicles, `Unassigned`, placement, durable existence [`canonical-vehicle-catalog`].
- [x] 1.2 **GREEN/REFACTOR:** Add Organization/Company contracts under `domain/catalog/` and `application/catalog/`.
- [x] 1.3 **RED:** Test Company candidate scope/repetition, admin binding, no identity creation [`provider-company-binding`].
- [x] 1.4 **GREEN/REFACTOR:** Add credential references in `bind-provider-company.ts`.
- [x] 1.5 **RED:** Test many-to-one Fleets, name rejection, review, union, enrichment, retention, placement conflict [`provider-fleet-binding`].
- [x] 1.6 **GREEN/REFACTOR:** Add `domain/catalog/{fleet-binding,union-projection}.ts` and reviews.
- [x] 1.7 **RED:** Test vehicle identity reuse, Company plate outcomes/conflicts, forbidden names, review [`external-identity-linking`].
- [x] 1.8 **GREEN/REFACTOR:** Add `domain/catalog/matching.ts` and vehicle review.
- [x] 1.9 **RED:** Test capability independence, five levels, fallback, defaults, absence, unavailability [`capability-source-precedence`].
- [x] 1.10 **GREEN/REFACTOR:** Add `domain/catalog/precedence.ts` and `SetCapabilityPolicy`.

## Phase 2: MongoDB

- [x] 2.1 **RED:** Replica-set test validators, indexes, Company `Unassigned` uniqueness.
- [x] 2.2 **GREEN/REFACTOR:** Add hierarchy/connection Mongo persistence/migrations.
- [x] 2.3 **RED:** Test identity uniqueness, many-to-one lookup, presence, reviews, policies, checkpoints.
- [x] 2.4 **GREEN/REFACTOR:** Add identity/review/policy/import Mongo repositories.
- [x] 2.5 **RED:** Test active-run uniqueness, lease claim/expiry, last-success, counts, crashes, absence indexes [`catalog-synchronization`].
- [x] 2.6 **GREEN/REFACTOR:** Add run/lease documents, repositories, validators, indexes, migrations.

## Phase 3: Providers

- [x] 3.1 **RED:** Test observed Cybermapa fields, scoped `gps_id`, no Fleet identity [`cybermapa-catalog-import`].
- [x] 3.2 **GREEN/REFACTOR:** Add Cybermapa client, mapper, source, credentials.
- [x] 3.3 **RED:** Test 5,542 candidates, binding, duplicate plates, resume, `Unassigned`, order/placement.
- [x] 3.4 **GREEN/REFACTOR:** Add batching/checkpoints in `application/catalog/import-catalog.ts`.
- [x] 3.5 **RED:** Test Howen fields, partial union, enrichment, retention, omission, concurrency, failure [`howen-catalog-import`].
- [x] 3.6 **GREEN/REFACTOR:** Add Howen mapper/source via shared contracts.

## Phase 4: Synchronization

- [x] 4.1 **RED:** Inject clock; test initial sync, six-hour boundary, freshness skip, shared outcomes, exclusion, isolation/retry, successful-full-snapshot-only reconciliation, failed/partial-preservation [`catalog-synchronization`].
- [x] 4.2 **GREEN/REFACTOR:** Add shared sync/due/status use cases; reuse `ImportCatalog`.
- [x] 4.3 **RED:** Test invalid cron secret, non-disclosure, connection isolation, retryable failure.
- [x] 4.4 **GREEN/REFACTOR:** Add internal sync Route Handler and constant-time authorization. `ProviderConnectionRepository.listAll()` added (enabled-as-existence decision, intentionally tenant-unscoped and pinned by test — see apply-progress). Composition root at `app/api/internal/catalog/synchronize/composition.ts` resolves the provider adapter server-side from `credentialRef`; only Cybermapa is wired to a real adapter today, Howen connections surface as an explicit `unsupported-provider`/`permanent:true` outcome, distinguishable in the response from a merely nonexistent connection — see task 5.2b for closing that gap. Risk #15 closed in `synchronize-catalog-connection.ts`. Carried-forward risks #16, #17, #18 recorded in apply-progress.

## Phase 5: Delivery and Live

- [x] 5.1 **RED:** Test same-origin/fresh-admin, bindings, reviews, manual sync, exclusion, status/counts. COMPLETE (3rd pass): all five route groups (list/resolve reviews, company-candidate binding, manual sync, sync status) are covered with mutation proof, closing carried-forward risk #3 fully. Status/counts is now covered by `GET /api/admin/catalog/connections/[connectionId]/status`, wired to the already-tenant-scoped, already-allowlist-projected `GetCatalogSyncStatus` (not widened). The freshness call-count assertion now spans all five routes (list, resolve, bind, sync, status).
- [x] 5.2 **GREEN/REFACTOR:** Add thin `app/api/admin/catalog/**` routes. COMPLETE (3rd pass) — closes the remaining gap from the 2nd pass plus a fresh-context review's four findings: (a) `GET /api/admin/catalog/connections/[connectionId]/status` wiring `GetCatalogSyncStatus` (blank-connectionId → 400, not-found/cross-tenant → 403 non-disclosure, found → 200 with the already-allowlisted `CatalogSyncStatus`, all mutation-proven). (b) **Bug fix**: the new-Vehicle path in `resolve-catalog-review.ts` was creating a Vehicle with no `plate`, even though `VehicleMatchReview.normalizedPlate` was available — `toActiveCompanyVehicle` (`import-catalog.ts`) excludes any vehicle with `plate === undefined` from the plate-matching candidate pool, so an admin-created Vehicle was permanently invisible to future automatic plate matching (same class of bug PR #25 fixed for the import path). Fixed by carrying `plate: review.normalizedPlate` through, mutation-proven. (c) Added a `fleetIdentities.save` call-count spy and mutation-proved a duplicate-write blind spot that a plain `.get()` assertion could not catch (a fixed-key, pure-function write is invisible to state-based assertions on a duplicate call — the same shape already caught and fixed for `vehicles.save` in the 2nd pass). (d) Two small coverage gaps closed: a genuinely nonexistent `targetId` on an otherwise-pending, correctly-scoped vehicle-match review (previously only cross-company and already-resolved-plus-missing were tested, and the latter is short-circuited earlier by the status guard — dropping the `vehicle &&` short-circuit would have thrown uncaught for this exact input, now caught); a blank `candidateId` test for the bind route (both sibling routes already had one, this one didn't). Reviewer-verified this pass: the kept `review.status !== "pending"` guard (2nd pass) is correct — the reviewer traced that the org-scope check at line 28 runs strictly before it, so cross-tenant opacity is unaffected either way, and confirmed the prior "removal changes zero test outcomes" claim was factually wrong. The Vehicle-only resolution-to-new + fleet-binding-rejects-new design (2nd pass) was independently confirmed against both specs, and reusing HTTP 400 for the unsupported case was confirmed consistent (zero existing 422 usage in this codebase). **RISK #22 (recorded here, CLOSED later in task 6.2 — see the Risk Registry):** `app/api/admin/catalog/composition.ts` now imports from `app/api/internal/catalog/synchronize/composition.ts` (for `resolveConnectionSource`/`createDefaultConnectionSourceFactories`) — two delivery composition roots now depend on each other. Not a behavior bug; shared provider-resolution logic arguably belongs outside either route's composition file. A consolidation candidate for slice 17. Also: `createDefaultConnectionSourceFactories` has no direct unit test — low risk, since it only recomposes already-tested pieces (`createCybermapaClient`/`readCybermapaConfig`/`createCybermapaImportSource`, each independently tested).
- [x] 5.2b **GREEN/REFACTOR:** Wire Howen to automatic cron synchronization — add the per-connection company-assignment domain field (or equivalent), its Mongo document/validator/migration, and cron composition wiring in `app/api/internal/catalog/synchronize/composition.ts` so Howen connections resolve to a real `CatalogImportSource` instead of `unsupported-provider` [`catalog-synchronization`, `howen-catalog-import`]. Closes carried-forward risk #16. **COMPLETE.** Groundwork (PR `feat/catalog-howen-cron`) plus the follow-up slice on `feat/catalog-connection-assignment` close all four findings from the prior fresh-context review: (1) CRITICAL — added `assignCompanyToConnection` (`domain/catalog/company-candidate.ts`), the `createAssignConnectionCompanyApplication` use case (`application/catalog/assign-connection-company.ts`, mirrors `bindProviderCompany`'s admin/tenant/`belongsToOrganization` checks exactly, full four-cell matrix mutation-proven including the success cell), and an authorized admin route `POST /api/admin/catalog/connections/[connectionId]/company` wired through `app/api/admin/catalog/composition.ts` — `ProviderConnectionRepository.save` now has a real, authorized production call site. (2) HIGH — `resolveCandidateCompanyId` (`application/catalog/import-catalog.ts`) now verifies a direct `candidate.companyId` belongs to the connection's tenant via `belongsToOrganization` before it ever reaches `computeVehiclePlacement`, defense-in-depth beyond the write-time gate; cross-tenant rejection mutation-proven. (3) HIGH — the manual sync route now returns three distinguishable Spanish outcomes (`unsupportedProvider`, `missingCompanyAssignment`, `providerMisconfigured`) via `classifyConnectionSourceProblem`; `catalog-admin.test.ts`'s mock now registers a real `howen` factory and the previously-vacuous "unsupported provider" test was fixed to use a genuinely unsupported connection. (4) MEDIUM — `classifyConnectionSourceProblem` (`app/api/internal/catalog/synchronize/composition.ts`) distinguishes `missing-company-assignment` (no `companyId`) from `misconfigured` (has `companyId`, factory still declines) in both `buildDueCandidates` (cron) and the admin sync route; both buckets mutation-proven distinguishable. End-to-end: `synchronize.test.ts`'s "schedules an enabled Howen connection ... through the same candidate pipeline as Cybermapa" plus the existing provider-agnostic six-hour-boundary tests in `synchronize-catalog-connection.test.ts`/`synchronize-due-catalog-connections.test.ts` confirm a Howen connection with an authorized company assignment is picked up by the scheduler, gets an initial sync, and becomes due again six hours later through the same `SynchronizeCatalogConnection` as Cybermapa — no provider conditional in `application/*` or `domain/*` (confirmed via grep). Fresh-context review of this closure PASSED CLEAN (reachability hand-traced HTTP → `assignConnectionCompany` → `connections.save`, confirmed the ONLY production call site under `app/`; independent-guard and `saveSpy` claims verified by hand-trace; 502 for misconfiguration confirmed consistent with the existing `retryable-failure` convention) and surfaced one perf fix, applied: `resolveCandidateCompanyId` now caches the `companies.findById` lookup per `companyId` (mirrors the existing `loadCompanyVehicles`/`loadCompanyFleets` pattern), cutting a full Howen sync from ~5,542 redundant identical-Company lookups to one; call-count-spy mutation-proven (N candidates sharing one `companyId` → exactly 1 call; removing the cache raises it to N). **Scope precision**: the import-time tenant check (finding 2) is architecturally generic — no provider conditional — but `map-cybermapa-catalog.ts` never sets `candidate.companyId` (only `companyLabel`), so Cybermapa never enters that branch in production; its tenant safety still comes entirely from `stageCompanyCandidate`/`bindProviderCompany` upstream. The practical blast radius of this check is Howen-only today. Diff vs `feat/catalog-howen-cron`: 363 changed lines, under the 400-line budget.
- [x] 5.3 **RED:** Test Spanish states, `Sync now`, freshness, counts, failures, accessibility. **COMPLETE** — closed on branch `feat/catalog-admin-ui-reviews` (base `feat/catalog-admin-ui`). Fixed three defects disclosed in PR #37's body: (1) stale status from a previous connection query surviving a subsequent failed query in both `consultarEstado` and `sincronizarAhora` — fixed by clearing `status` on error in both handlers and rendering `status.connectionId` (`Conexión: {id}`) so the visible status can never silently mismatch the queried connection; mutation-proven for both handlers via a compound query-A-succeeds/query-B-fails scenario. (2) the empty-`latestRun` branch (`Todavía no hay sincronizaciones registradas.`) was implemented but had zero test coverage — added a dedicated test and mutation-proved it (the branch was already correct; this closes a coverage gap, not a code bug). (3) `CatalogAdminScreen` was never mounted by any test (`page.test.tsx` called `CatalogAdminPage()` as a plain function, never RTL `render()`) — added a real `render(await CatalogAdminPage())` mount test asserting page → screen → all three panels (connection sync, company-candidate binding, pending reviews) render together; mutation-proven by removing `<CatalogAdminScreen />` from `page.tsx` and by removing a panel from the screen. Then built the deferred pending-reviews UI: lists pending reviews with Spanish subject/state labels, resolves to an existing target for both `fleet-binding` and `vehicle-match` subjects, and resolves to a new Vehicle for `vehicle-match` only (the "resolve to new" control is not rendered at all for `fleet-binding` reviews, since `resolveCatalogReview` returns `unsupported`/400 for that combination — a proactive Spanish-labelled UI guard rather than reactive parsing of the generic `Solicitud inválida.` 400 body, which is also returned for unrelated bad-request cases and can't be reliably distinguished after the fact). All 8 new/changed conditionals across `connection-sync-panel.tsx`, `review-item.tsx`, and `pending-reviews-panel.tsx` are mutation-proven both-branch (see apply-progress for the full enumeration and verbatim proofs).
- [x] 5.4 **GREEN/REFACTOR:** Build focused `app/admin/catalog/**` UI. **COMPLETE** — this slice adds `review-item.tsx` (46 lines, single review row: subject/state labels, raw candidate ids per the review-summary contract, existing-target resolution for both subjects, new-Vehicle resolution gated to `vehicle-match` only) and `pending-reviews-panel.tsx` (36 lines, list container: manual load, Spanish empty/error states, removes a review from the list on successful resolution without re-fetching), wired into `catalog-admin-screen.tsx` as a third section. Fixes finding 1 in `connection-sync-panel.tsx` (stale-status clearing + `connectionId` rendering, +1 line net) and finding 3 in `page.test.tsx` (render-level mount test, +11 lines). No API reshape — `GET /api/admin/catalog/reviews` and `POST /api/admin/catalog/reviews/[reviewId]/resolve` used as-is. No provider conditional (grep-confirmed zero matches for `howen|cybermapa|provider ===|providerType` across `app/admin/catalog`). No `data-testid` (grep-confirmed zero matches). No inline styles (grep-confirmed zero matches). Diff vs `feat/catalog-admin-ui`: 256 changed lines (9 files: 4 new, 5 modified; `.atl/skill-registry.md`'s pre-existing unrelated modification excluded), under the 400-line budget — no split needed. **Open question carried forward, not a blocker**: `CatalogReviewSummary.candidateFleetIds`/`candidateVehicleIds` are raw ids with no label projection; rendered as-is per instructions. See apply-progress for the recommendation on whether a label projection is needed.

**Fresh-context review (2nd pass) closure**: all three PR #37 defects independently verified genuinely fixed (stale-status mutation-proof traced for both handlers; empty-`latestRun` branch confirmed rendering and asserted; `page.test.tsx` confirmed to perform a real `render(await CatalogAdminPage())` asserting a control unique to each of the three panels). The fleet-binding "resolve to new" prevention was upheld with a stronger argument than originally given: the domain has no concept of creating a new Fleet at all — `resolveCatalogReviewToFleet` only ever accepts an existing `fleetId`, and `provider-fleet-binding/spec.md` only ever describes an administrator selecting a Fleet, so the capability doesn't exist anywhere in the stack (not merely hidden from admins who might need it). The `onResolved` call-count spy was verified real and discriminating; zero false proofs. Four additional findings closed this pass: (1) **accessibility** — `review-item.tsx`/`pending-reviews-panel.tsx` had zero `role="status"` while sibling panels (`connection-sync-panel.tsx`, `company-candidate-binding-panel.tsx`) announce mutating-action success that way; added a `role="status"` "Revisión resuelta." announcement in `pending-reviews-panel.tsx` (the resolved row itself unmounts on success, so the announcement is promoted to the surviving parent panel rather than the child), mutation-proven twice (render guard removed, `setNotice` call site removed). (2) **untrimmed target id** — `review-item.tsx`'s enable-check trimmed (`!targetId.trim()`) but `resolver({ targetId })` sent the raw state, so `" fleet-a "` would reach the server verbatim and produce a spurious not-found; fixed to send `targetId.trim()`. The same pre-existing pattern in `company-candidate-binding-panel.tsx` (`candidateId` in the URL, `companyId` in the body) was fixed too — both mutation-proven. **Note**: the identical untrimmed-value pattern also exists in `connection-sync-panel.tsx` (three call sites: `connectionId` in three URLs, `companyId` in the company-assignment body) — discovered but NOT fixed this pass, since it was outside the four findings given; flagged for a follow-up pass. (3) `connection-sync-panel.tsx:39` — the untested false branch where the post-sync status refresh itself fails; covered with a new test AND fixed (clears `status` on refresh failure too, closing the same stale-status class this slice already fixed for the two primary handlers), mutation-proven. (4) `REVIEW_STATUS_LABELS.resolved` was untested and architecturally unreachable (`GET /api/admin/catalog/reviews` only ever returns pending reviews, and a resolved review is removed from local state rather than re-rendered with its post-resolution payload) — **decided: deleted**, rather than adding a synthetic test for a state this component is structurally incapable of receiving; `review-item.tsx` now renders "Pendiente" as a fixed literal instead of a lookup with a dead branch. **RISK #24 (recorded here, CLOSED later in task 6.2 — see the Risk Registry):** `app/api/admin/catalog/delivery.ts`'s `badRequest()` collapses every 400 outcome — including `resolveCatalogReview`'s domain-level `unsupported` resolution outcome — into one indistinguishable `{error: "Solicitud inválida."}`, unlike `unsupportedProvider`/`missingCompanyAssignment`/`providerMisconfigured`, which each get a distinct status and message. A client cannot tell an unsupported resolution from a malformed body. Owned by slice 17's quality gate. **Recorded, no action**: `candidateIds?.length` with `candidateIds` undefined, and `result.reviews ?? []` in `pending-reviews-panel.tsx`, are defensive branches unreachable under the real API contract (`toReviewSummary` always includes the relevant candidate-id array; the list route always returns an array) — kept as defensive code, not covered. **Recorded for product**: an admin resolving a review sees raw candidate ids (`candidateFleetIds`/`candidateVehicleIds`), not labels — confirmed by the reviewer as a genuine product gap rather than an oversight; a label projection is a real scope addition, deliberately deferred as a decision rather than silently skipped. Diff vs `feat/catalog-admin-ui` after this pass: 298 changed lines, still under the 400-line budget.
- [x] 5.5 **RED:** Test union projection, canonical identity, source-local capability loss, and fallback [`live-core-contracts`]. COMPLETE. `unionFleetRoster` and `resolveCapabilitySource` are reused from `domain/catalog`, not re-derived; the union fixture discriminates against intersection, first-roster-wins, and two-way-only union. A corrected diagnosis is recorded: the `if (status)` guard in `resolveVehicleCapabilities` was first reported as a false proof because no `CapabilitySourceStatus` value is falsy — true, but the wrong question. Its real job is to stop an `undefined` status overwriting a set one under the same provider key when two identities share a provider. Regression test added and mutation-proven.
- [x] 5.6 **GREEN/REFACTOR:** Add `project-canonical-live.ts` and switched live composition. COMPLETE across PRs #39, #40, and the seam PR. **RISK #25 (recorded, not implemented):** `aggregate-operational-sources.ts`'s collision guard rejects a source only on string-identical ids. Howen's raw source emits `howen:vehicle:${deviceNumber}`; the canonical projection emits the catalog's own `vehicle.id`. If both were ever active together the same physical vehicle surfaces under two ids, the guard misses it, and two live tiles appear for one vehicle — violating AGENTS.md's duplicate-tile rule. Before any PR wires a production loader, the seam must REPLACE the Howen source rather than add to it, or the guard must compare canonical identity. Whoever activates the switch owns proving it. Note also that `app/live/page.tsx` calls `createOperationalSources(runtime)` with one argument, so `canonicalCatalogSource` is `undefined` and the projection is not yet reachable in production — a deliberate feature switch per `design.md`, acceptable only while a loader is genuinely pending.
- [x] 5.7 **GREEN/REFACTOR:** Carry `operationalAlerts` and `videoAlerts` through to the live contracts. **COMPLETE.** Both capabilities already resolved in `resolveVehicleCapabilities` (`application/live/project-canonical-live.ts`) and were discarded because `LiveVehicleState` had no field for them. Added two new optional fields to `LiveVehicleState` (`application/live/contracts.ts`) — `operationalAlerts`/`videoAlerts`, typed with a new local `CapabilityAvailability` union (`{kind:"resolved", source: string} | {kind:"unavailable"}`) declared in `contracts.ts` itself, not imported from `domain/catalog`, so the live module's core contracts stay decoupled from catalog/provider types; only `project-canonical-live.ts` bridges the two. `projectVehicle` now passes `capabilities.operationalAlerts`/`capabilities.videoAlerts` straight through — zero new conditionals, a pure capability-mapping wire-through reusing `domain/catalog/precedence.ts`'s already-computed resolution rather than re-deriving an order or default. All four capabilities (`gps`, `video`, `operationalAlerts`, `videoAlerts`) are now independently resolved AND observable end to end; proven with a 4-case `it.each` in `project-canonical-live.test.ts` that degrades exactly one capability at a time and asserts the other three stay resolved. Three mutation proofs on the new mapping, all caught: swapping the two alert assignments; hardcoding both to `{kind:"unavailable"}`; omitting the fields entirely (reproducing the original RED). Existing `video`/`gps` behavior is unchanged — `findSnapshot`/`videoSnapshot`/`gpsSnapshot` untouched; the one existing full-object-equality test (`projectCanonicalLive — canonical identity`) was updated to include the two newly-populated fields, since its fixture already resolved both alert capabilities via pre-existing `capabilityStates`. No component consumes the new fields yet (grep-confirmed across `components/live` and `application/live`), so nothing is forced to branch on provider to read alert availability — checking `kind === "resolved"` alone is enough; `source` is traceability metadata only, mirroring `Device.provider`'s existing, already-accepted convention (used today for the sidebar's provider filter, not for rendering-strategy decisions). `docs/architecture/03-live-core-domain.md`'s "Canonical catalog projection" section updated to describe the shipped fields instead of "not-yet-implemented" [`live-core-contracts`, `capability-source-precedence`].

## Phase 6: Release

- [x] 6.1 Update docs `03`, `05`, cron environment: cadence, security, rollout/rollback. **COMPLETE.** Doc `03` gained a "Canonical catalog projection" section describing `project-canonical-live.ts`'s mapping into `Device`/`DeviceTelemetry`, and disclosing that `operationalAlerts`/`videoAlerts` resolve but have no contract sink yet (task 5.7). Doc `05` gained a "Canonical catalog composition switch" section describing the `canonicalCatalogSource` seam in `create-operational-sources.ts`, disclosing that `app/live/page.tsx` calls it with one argument (no production loader wired), and restating RISK #25's duplicate-tile guard warning for whoever wires that loader. New `docs/architecture/08-catalog-synchronization.md` documents cadence (6-hour due window, cron polls repeatedly, manual bypasses the due gate entirely — a manual trigger never re-checks freshness, since the post-lease recheck is gated behind `trigger === "scheduled"`; what stops a manual and a scheduled trigger racing each other is the per-connection lease, while the post-lease recheck protects the distinct sequential case of a stale due-candidate list, and the doc states both mechanisms and warns against treating either as a substitute for the other), security (Node runtime, constant-time secret, same-origin admin auth for manual), and rollout/rollback verbatim from `design.md`'s Migration and Rollback section.
- [x] 6.2 Run lint, typecheck, tests, coverage, build; record <=400-line diffs. **COMPLETE.** All five gate commands pass clean (lint: 0 errors; typecheck: 0 errors; tests: 866/866 across 101 files; coverage: 94.23% stmts/87.76% branch/95.56% funcs/96.38% lines, no failures; build: compiles, all 22 routes generated, zero errors — the build had never been run before in this change and this is its first clean pass). Diff vs `feat/catalog-live-seam` (excluding `.atl/skill-registry.md`): 15 files, 207 insertions + 70 deletions = 277 changed lines, under the 400-line budget. The build generates 21 project-defined App Router entries plus the framework's `_not-found`. **Risks closed this slice:** #21 (translated `app/api/admin/users`'s English `badRequest`/`lastAdmin`/inline "Forbidden." strings to Spanish, added a shared `adminForbidden()` matching the existing `auth/delivery.ts`/`catalog/delivery.ts` Spanish "No tenés permisos..." string, deduplicating 4 call sites); #22 (extracted `resolveConnectionSource`/`classifyConnectionSourceProblem`/`createDefaultConnectionSourceFactories`/`createHowenConnectionFactory` into new `app/api/catalog/connection-sources.ts`; `internal/catalog/synchronize/composition.ts` now imports and re-exports from it instead of `admin/catalog/composition.ts` importing from `internal/catalog/synchronize/composition.ts` — the two delivery composition roots no longer depend on each other, both depend on the new shared module; `synchronize.test.ts`'s `vi.mock("./composition", ...importOriginal)` pattern still resolves these as real exports of the module namespace, unmodified); #24 (added `unsupportedResolution()` — 409, Spanish — to `app/api/admin/catalog/delivery.ts`; `resolve/route.ts`'s `case "unsupported"` now returns it instead of the generic `badRequest()` 400, so a domain-level "this review type can't resolve to new" is distinguishable from a malformed request; test updated to assert 409). **Risk NOT closed, explicitly deferred:** #23 — `domain/catalog/sync-run.ts` discarding structured failure facts at write time and the admin UI's hand-duplicated reverse-parse map. Not attempted: reshaping a persisted document's write/read path is a real behavior change (new field(s), a migration concern, and at least one more call site to update in the admin UI), assessed as materially larger than the ~270-line budget already spent on docs plus risks #21/#22/#24, and is explicitly called out in the delegating instructions as "almost certainly too large for this slice." Recorded here as still open for a future slice.

## Risk Registry

Standalone ledger for risks #1-#25 raised across this change's PR chain (#6-#42). Each entry stands on its own — the PRs referenced below are unmerged and could be closed or deleted, so this table, not the PR bodies, is the durable record. Compiled by re-reading every PR body via `gh pr view --json body` rather than trusting any single prior summary.

| # | Description | State |
|---|---|---|
| 1 | `createCompany` performed two non-atomic writes (Company, then its `Unassigned` Fleet); a mid-write failure could orphan a Company with no `Unassigned` Fleet. | CLOSED — PR #14 (wrapped both writes in one transaction). |
| 2 | `stageCompanyCandidate` trusted a caller-supplied `ProviderConnection.organizationId` with no repository lookup, allowing a forged tenant id. | CLOSED — PR #28 (resolves the connection via `connections.findById` before anything else runs). |
| 3 | Fleet-binding and vehicle-match review resolution had no authorization enforcement or test coverage at all. | CLOSED — PR #32 (full four-cell admin/tenant authorization matrix on the resolve route, mutation-proven). |
| 4 | `normalizePlate` (`domain/catalog/matching.ts`) strips all hyphens/whitespace regardless of position, so two differently-hyphenated plates with the same underlying characters normalize identically. | OPEN — owner: unassigned, future slice. |
| 5 | `application/catalog/resolve-catalog-review.ts`'s admin-resolution path re-implements company-scope checking inline instead of calling `domain/catalog/matching.ts`'s scoped matcher — a parallel implementation of the same rule that can drift. | OPEN — owner: unassigned, future slice. |
| 6 | `ProviderConnection.credentialRef` had no structural constraint (bare `string`), so a raw secret could be stored as if it were a reference. | CLOSED — PR #19 (`CREDENTIAL_REF_PATTERN`, a closed reference grammar). |
| 7 | Mongo persistence for pending reviews, capability policies, capability states, and import-item checkpoints (the remainder of tasks 2.3/2.4) was still outstanding after slice 6 (PR #14). | CLOSED — PR #16 (reviews + capability policies) and PR #17 (capability states + import-item checkpoints). |
| 8 | `resolvePlateMatch`'s conflict check treated an identity marked `presence: "absent"` (e.g. a decommissioned device) the same as an active one, blocking a replacement device's auto-link. | CLOSED — PR #17 (conflict check now excludes absent identities). |
| 9 | `shouldReconcileCatalogSyncAbsence` needed proof that only a final, successful, full-snapshot sync may mark identities absent — otherwise a partial or failed run could wrongly reconcile absence. | CLOSED — PR #28 (gated on `status === "succeeded" && fullSnapshot === true`, proven). |
| 10 | The Cybermapa client sends the account password raw in the POST body (`pwd: config.password`); this matches the documented external contract but adds no protection on this side. | OPEN — owner: unassigned, future slice. |
| 11 | `CREDENTIAL_REF_PATTERN` is a format-only grammar; it cannot semantically distinguish an opaque reference from a secret that merely happens to match the shape. | OPEN — owner: unassigned, future slice. |
| 12 | Checkpoint resume only helps within a single run's own retry loop; every new trigger after a failure starts a brand-new `runId` and always re-fetches/re-walks the full snapshot from scratch. | OPEN — owner: unassigned, future slice. |
| 13 | `reconcilePlacement` protected only `source: "admin"` placements, so a real-Fleet system-sourced placement could be silently reset to `Unassigned` by a later source supplying no fleet. | CLOSED — PR #25 (`feat/catalog-union-placement`; recorded there as "Finding 2", not tagged by number). |
| 14 | Howen adapter failures collapse to one generic `category: "internal"` for every failure mode (network, expired session, bad status, parse failure), unlike Cybermapa's finer-grained `statusCategory()`. | OPEN — owner: unassigned, future slice. |
| 15 | `synchronize-catalog-connection.ts` did not wrap `importer.importCatalog(...)` in a try/catch, so a thrown (not returned) failure from `loadCompleteSnapshot` could leave a claimed lease unreleased except by TTL. | CLOSED — PR #31 (wraps the call so every failure flows through the existing lease-releasing failure path). |
| 16 | Howen connections had no company-assignment field, so cron synchronization always resolved them to `unsupported-provider` — Howen was never actually wired to automatic sync. | CLOSED — PR #35 (groundwork) + PR #36 (`assignCompanyToConnection`, authorized admin route, cron composition wiring). |
| 17 | `ProviderConnection` has no `enabled`/`disabled` toggle; the cron sweep's "every enabled connection" currently means "every connection that exists". | OPEN — owner: unassigned, future slice. Wording clarified this slice in `docs/architecture/08-catalog-synchronization.md`; the toggle itself remains unimplemented. |
| 18 | Every tenant currently shares one global Cybermapa login (`CYBERMAPA_USER`/`CYBERMAPA_PASSWORD` from `process.env`), despite the per-connection `credentialRef` field implying tenant-scoped credentials. | OPEN — owner: unassigned, future slice. |
| 19 | A vehicle-match review resolution creates an external Vehicle identity without an independent existence check, on BOTH the existing-target and new-target paths — not only the new one. Because `external_vehicle_identities` carries a unique index on `{organizationId, connectionId, entityKind, externalId}`, resolving a pending review after a later sync run has independently created an identity for the same `externalId` raises a Mongo duplicate-key error rather than failing gracefully. | OPEN — owner: unassigned, future slice. Scope widened by the second verification pass. |
| 20 | Narrowed by the second verification pass; the earlier wording overstated it. The LEASE claim path IS proven under real concurrency — `integrations/persistence/mongodb/catalog-mongodb.test.ts` races two claimants with `Promise.all` against a real `MongoMemoryReplSet`. What remains unproven under real concurrency is the review-resolution and vehicle-identity write paths, which still rely on single-process interleaving tests. | OPEN (narrowed) — owner: unassigned, future slice. |
| 21 | `app/api/admin/users` had English user-facing strings (`badRequest`, `lastAdmin`, four inline `"Forbidden."` literals), inconsistent with the rest of the Spanish admin UI. | CLOSED — task 6.2 slice (shared `adminForbidden()` matching the existing Spanish convention). |
| 22 | `app/api/admin/catalog/composition.ts` imported from `app/api/internal/catalog/synchronize/composition.ts`, making the two delivery composition roots mutually dependent. | CLOSED — task 6.2 slice (shared logic extracted to `app/api/catalog/connection-sources.ts`). |
| 23 | `domain/catalog/sync-run.ts` discards structured failure facts at write time, persisting only English prose, forcing the admin UI to reverse-parse it via a hand-duplicated map. | OPEN — owner: unassigned, future slice. Explicitly deferred at task 6.2 as a persisted-document reshape (migration concern), assessed too large for that slice's budget. |
| 24 | `app/api/admin/catalog/delivery.ts`'s `badRequest()` collapsed every 400 outcome — including a domain-level "unsupported resolution" result — into one indistinguishable body. | CLOSED — task 6.2 slice (added a distinct `unsupportedResolution()`, 409). |
| 25 | `aggregate-operational-sources.ts`'s collision guard compares source ids as raw strings; Howen's raw id (`howen:vehicle:${deviceNumber}`) and the canonical projection's id (`vehicle.id`) would never collide for the same physical vehicle if both sources were active together, risking two live tiles for one vehicle. | OPEN — owner: whoever wires the first production canonical-catalog loader. The seam must REPLACE the Howen source rather than add to it, or the guard must compare canonical identity, before that happens. |

Note on numbering provenance: risk #7 does not appear in either the closed or carried-forward summaries this change had been carrying (both lists total 24, not 25). Re-reading the PR chain directly (`gh pr view --json body`) resolved the gap: PR #9 introduced items 1-3, PR #14 closed #1 and introduced one new item, and PR #15 explicitly references "items 2-6" of a six-item carry-forward list before adding "new #7" — a count that does not reconcile against what PR #9 and PR #14's bodies literally enumerate. This confirms different chained-PR authors did not maintain one continuously-incremented ledger before it stabilized around PR #17 onward (the numbering the rest of this registry, and every later PR, actually uses). #7 itself is traceable and closed regardless of that inconsistency — see the row above.
