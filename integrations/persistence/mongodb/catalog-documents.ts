import type { ObjectId } from "mongodb";
import type { GlobalCatalogReview, GlobalVehicle, ProviderConnection, ProviderContribution, ProviderDefinition, ProviderFleetMembership, TenantVehicleGrant, SentinelGroup, GroupEvidenceBinding } from "@/domain/catalog";
import type { CapabilitySourceStatus } from "@/domain/catalog/capabilities";
import type { GlobalSyncRun } from "@/application/catalog/synchronize-global-connection";

type Timestamps = { createdAt: Date; updatedAt: Date };
export type CatalogVehicleDocument = { _id?: ObjectId; schemaVersion: number; id: string; normalizedPlate: string; plate: string; placementFleetId: string; placement?: GlobalVehicle["placement"] } & Timestamps;
export type CatalogGroupDocument = { _id?: ObjectId; schemaVersion: number; id: string; label: string } & Timestamps;
export type GroupEvidenceBindingDocument = { _id?: ObjectId; schemaVersion: number; id: string; groupId: string; evidence: GroupEvidenceBinding["evidence"] } & Timestamps;
export type ProviderDefinitionDocument = { _id?: ObjectId; schemaVersion: number; id: string; adapterKey: string; capabilities: string[] } & Timestamps;
export type ProviderConnectionDocument = { _id?: ObjectId; schemaVersion: number; id: string; providerId: string; credentialRef: string; enabled: boolean; cadenceMinutes: number } & Timestamps;
export type ProviderContributionDocument = { _id?: ObjectId; schemaVersion: number; id: string; connectionId: string; externalId: string; vehicleId: string; capabilities: Partial<Record<string, CapabilitySourceStatus>>; presence: "present" | "absent" } & Timestamps;
export type ProviderFleetMembershipDocument = { _id?: ObjectId; schemaVersion: number; connectionId: string; externalFleetId: string; vehicleId: string; label: string } & Timestamps;
export type OrganizationVehicleAccessDocument = { _id?: ObjectId; schemaVersion: number; organizationId: string; vehicleId: string } & Timestamps;
export type CatalogReviewDocument = { _id?: ObjectId; schemaVersion: number; id: string; connectionId: string; externalId: string; subject: "vehicle-identity"; reason: string; normalizedPlate?: string; evidenceKey?: string; candidateGroupIds?: string[]; candidateVehicleIds: string[]; status: "pending" | "resolved"; resolvedVehicleId?: string } & Timestamps;
export type CatalogSyncRunDocument = Omit<GlobalSyncRun, "startedAt" | "completedAt"> & { _id?: ObjectId; schemaVersion: number; startedAt: Date; completedAt?: Date } & Timestamps;
export type CatalogSyncLeaseDocument = { _id?: ObjectId; schemaVersion: number; connectionId: string; runId: string; leaseUntil: Date; createdAt: Date; updatedAt: Date };

export function toCatalogVehicleDomain(document: CatalogVehicleDocument): GlobalVehicle { return { id: document.id, normalizedPlate: document.normalizedPlate, plate: document.plate, placementFleetId: document.placementFleetId, ...(document.placement ? { placement: { ...document.placement, assignedAt: new Date(document.placement.assignedAt) } } : {}) }; }
export function toCatalogVehicleDocument(value: GlobalVehicle, now: Date, existing?: CatalogVehicleDocument): CatalogVehicleDocument { return { schemaVersion: 2, ...value, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toCatalogGroupDomain(document: CatalogGroupDocument): SentinelGroup { return { id: document.id, label: document.label }; }
export function toCatalogGroupDocument(value: SentinelGroup, now: Date, existing?: CatalogGroupDocument): CatalogGroupDocument { return { schemaVersion: 2, ...value, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toGroupEvidenceBindingDomain(document: GroupEvidenceBindingDocument): GroupEvidenceBinding { return { id: document.id, groupId: document.groupId, evidence: { ...document.evidence } }; }
export function toGroupEvidenceBindingDocument(value: GroupEvidenceBinding, now: Date, existing?: GroupEvidenceBindingDocument): GroupEvidenceBindingDocument { return { schemaVersion: 2, ...value, evidence: { ...value.evidence }, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toProviderDefinitionDomain(document: ProviderDefinitionDocument): ProviderDefinition { return { id: document.id, adapterKey: document.adapterKey, capabilities: [...document.capabilities] }; }
export function toProviderDefinitionDocument(value: ProviderDefinition, now: Date, existing?: ProviderDefinitionDocument): ProviderDefinitionDocument { return { schemaVersion: 2, id: value.id, adapterKey: value.adapterKey, capabilities: [...value.capabilities], createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toProviderConnectionDomain(document: ProviderConnectionDocument): ProviderConnection { return { id: document.id, providerId: document.providerId, credentialRef: document.credentialRef, enabled: document.enabled, cadenceMinutes: document.cadenceMinutes }; }
export function toProviderConnectionDocument(value: ProviderConnection, now: Date, existing?: ProviderConnectionDocument): ProviderConnectionDocument { return { schemaVersion: 2, ...value, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toProviderContributionDomain(document: ProviderContributionDocument): ProviderContribution { return { id: document.id, connectionId: document.connectionId, externalId: document.externalId, vehicleId: document.vehicleId, capabilities: { ...document.capabilities }, presence: document.presence }; }
export function toProviderContributionDocument(value: ProviderContribution, now: Date, existing?: ProviderContributionDocument): ProviderContributionDocument { return { schemaVersion: 2, id: value.id, connectionId: value.connectionId, externalId: value.externalId, vehicleId: value.vehicleId, capabilities: { ...value.capabilities }, presence: value.presence, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toProviderFleetMembershipDomain(document: ProviderFleetMembershipDocument): ProviderFleetMembership { return { connectionId: document.connectionId, externalFleetId: document.externalFleetId, vehicleId: document.vehicleId, label: document.label }; }
export function toProviderFleetMembershipDocument(value: ProviderFleetMembership, now: Date, existing?: ProviderFleetMembershipDocument): ProviderFleetMembershipDocument { return { schemaVersion: 2, ...value, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toOrganizationVehicleAccessDomain(document: OrganizationVehicleAccessDocument): TenantVehicleGrant { return { organizationId: document.organizationId, vehicleId: document.vehicleId }; }
export function toOrganizationVehicleAccessDocument(value: TenantVehicleGrant, now: Date, existing?: OrganizationVehicleAccessDocument): OrganizationVehicleAccessDocument { return { schemaVersion: 2, ...value, createdAt: existing?.createdAt ?? now, updatedAt: now }; }
export function toCatalogReviewDomain(document: CatalogReviewDocument): GlobalCatalogReview { return { id: document.id, subject: "vehicle-identity", connectionId: document.connectionId, externalId: document.externalId, reason: document.reason as GlobalCatalogReview["reason"], ...(document.normalizedPlate !== undefined ? { normalizedPlate: document.normalizedPlate } : {}), ...(document.evidenceKey !== undefined ? { evidenceKey: document.evidenceKey } : {}), ...(document.candidateGroupIds !== undefined ? { candidateGroupIds: [...document.candidateGroupIds] } : {}), candidateVehicleIds: [...document.candidateVehicleIds], status: document.status, ...(document.resolvedVehicleId !== undefined ? { resolvedVehicleId: document.resolvedVehicleId } : {}) }; }
export function toCatalogReviewDocument(value: GlobalCatalogReview, now: Date, existing?: CatalogReviewDocument): CatalogReviewDocument { return { schemaVersion: 2, id: value.id, subject: "vehicle-identity", connectionId: value.connectionId, externalId: value.externalId, reason: value.reason, ...(value.normalizedPlate !== undefined ? { normalizedPlate: value.normalizedPlate } : {}), ...(value.evidenceKey !== undefined ? { evidenceKey: value.evidenceKey } : {}), ...(value.candidateGroupIds !== undefined ? { candidateGroupIds: [...value.candidateGroupIds] } : {}), candidateVehicleIds: [...value.candidateVehicleIds], status: value.status, ...(value.resolvedVehicleId !== undefined ? { resolvedVehicleId: value.resolvedVehicleId } : {}), createdAt: existing?.createdAt ?? now, updatedAt: now }; }



